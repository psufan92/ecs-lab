version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "<TASK-DEFINITION>"
        LoadBalancerInfo:
          ContainerName: "sample-website"
          ContainerPort: 80